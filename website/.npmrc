# Keep in sync with the repository-root `.npmrc`. `npm --prefix website` reads
# this file as its project config, not the parent.
#
# Supply-chain cooldown (npm 11.10+). See the root `.npmrc` for the full rationale.
min-release-age=2
